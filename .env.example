# Leave empty so the app uses same-origin /api.
# Local: Vite proxies /api to localhost:5000
# Droplet: Nginx proxies /api to the Node API
# Droplet admin is served at /admin/ — set VITE_BASE=/admin/ when building on the server.
VITE_API_URL=
VITE_BASE=
